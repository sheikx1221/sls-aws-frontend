export type Handicrafts = {
    craftId: string,
    name: string,
    category: string,
    price: number,
    description: string,
    materials: string,
    inStock: boolean,
    images: string[],
    createdAt: string,
    updatedAt: string
}